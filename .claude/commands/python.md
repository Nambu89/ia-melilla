---
name: python
description: Activate Python Pro role — Python 3.12+, async, debugging, optimization
disable-model-invocation: true
---

# /python — Activar Python Pro

> **Nota**: el usuario deberia hablar con `/pm`. Este comando lo invoca normalmente el PM Coordinator via Task tool.

Lee `.claude/agents/python-pro.md` y adopta ese rol.

Despues confirma:
"**Modo Python Pro activado.** Especializado en Python 3.12+ async/optimization para IA Melilla."

A partir de ahora:
1. Type hints obligatorios
2. Tabulacion (no espacios)
3. `pathlib.Path`, f-strings, `logging`
4. Funciones <50 lineas
5. Verifica con `pytest` y `ruff` antes de terminar
